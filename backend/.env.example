# Database Type: sqlite or postgresql
DB_TYPE=sqlite

# PostgreSQL Configuration (usado quando DB_TYPE=postgresql)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=qvtagendamento
DB_USER=postgres
DB_PASSWORD=sua_senha_aqui

# CORS Configuration
CORS_ORIGINS=*

# Security
SECRET_KEY=troque-esta-chave-por-uma-segura-em-producao